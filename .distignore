# Files excluded from the production zip.
# Used by the GitHub Actions deploy workflow (rsync --exclude-from).
# Keep in sync with .gitignore for dev-only artifacts.

.git
.github
.distignore
.gitignore
.phpcs.xml
.editorconfig

# Development & testing
tests/
phpunit.xml
phpunit.xml.dist
composer.json
composer.lock
vendor/

# Documentation (lives in repo, not in deployed plugin)
ARCHITECTURE.md
CONVENTIONS.md
CHANGELOG.md
CODE_OF_CONDUCT.md
CONTRIBUTING.md

# CI/CD tooling
scripts/
node_modules/
package.json
package-lock.json

# OS artifacts
.DS_Store
Thumbs.db
*.log
